[project]
name = "pyblisher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dacite>=1.8.1",
    "httpx>=0.28.1",
    "pyright>=1.1.392.post0",
    "ruff>=0.9.3",
]

[tool.ruff]
line-length = 80
select = ["E", "F", "I"]

[tool.ruff.format]
quote-style = "single"

[tool.pyright]
venvPath = "."
venv = ".venv"
